# 可选：生产环境可叠加 Redis、对象存储、独立向量服务。
# 当前 MVP 使用本地 Chroma + SQLite，不依赖本文件即可运行。

services:
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  # minio:
  #   image: minio/minio
  #   command: server /data
  #   environment:
  #     MINIO_ROOT_USER: minio
  #     MINIO_ROOT_PASSWORD: minio12345
  #   ports:
  #     - "9000:9000"
  #   volumes:
  #     - minio_data:/data
  # volumes:
  #   minio_data: {}

  placeholder:
    image: busybox:1.36
    command: ["sh", "-c", "echo finance-rag-ft docker example && sleep infinity"]
